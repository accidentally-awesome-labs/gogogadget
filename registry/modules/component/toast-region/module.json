{
  "schema": 1,
  "module": {
    "id": "component/toast-region",
    "kind": "component",
    "name": "toast-region",
    "revision": 1,
    "contract": 1,
    "title": "Toast region",
    "description": "The ToastRegion renderer and its options.",
    "requires": [
      "component/toast",
      "element/ui-core"
    ],
    "files": [
      {
        "source": "internal/web/templates/ui/toast-region.templ",
        "target": "internal/web/templates/ui/toast-region.templ",
        "class": "templ",
        "sha256": "addd3973adac7dc3310bc07c60191ff8830c8d9a1b44aad22c728885f7c76433",
        "rewrite_module": true,
        "contract": true
      }
    ],
    "claims": {},
    "runtime": {
      "ui": [
        {
          "name": "toast-region",
          "family": "feedback",
          "engine": "alpine",
          "signature": "templ ToastRegion(o ToastRegionOpts)",
          "summary": "Renders the shell's live toast stack.",
          "guidance": "One per document, rendered in the shell and outside #content: navigation swaps only #content, so a region inside it would be destroyed mid-swap and lose both its queued toasts and its document listener. The live-region semantics sit on the container rather than on each toast, because a live region has to exist before its message arrives or nothing is announced. Its Alpine controller (toastRoot) is registered by the shell in static/app.js: the shell owns the HX-Trigger listener and the flash handoff across a redirect.",
          "states": [
            "default"
          ]
        }
      ]
    },
    "migrations": [],
    "environment": [],
    "docs": [],
    "tests": {},
    "data": [],
    "removal_policy": "free"
  }
}
